<nav class="bg-white sticky top-0 z-50" data-controller="navbar">

  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-shrink-0 flex items-center">
        <%= link_to root_path do %>
          <%= image_tag "logo.png", alt: "Logo", class: "h-14" %>
          <%#= image_tag "logo2.jpeg", alt: "Logo", class: "h-14" %>
        <% end %>
      </div>
      <div class="hidden sm:block sm:ml-6">
        <div class="flex space-x-4">
          <%= link_to "Services", services_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <%= link_to "Contact", contacts_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% if current_user && current_user.email == 'admin@gmail.com' %>
            <%= link_to 'Bookings', bookings_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% end %>
          <% if user_signed_in? %>
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% end %>
        </div>
      </div>
      <div class="sm:hidden">
        <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-indigo-800 hover:text-indigo-800 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" data-action="click->navbar#toggleMenu">
          <!-- Icon when menu is closed -->
          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-navbar-target="openIcon">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <!-- Icon when menu is open -->
          <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-navbar-target="closeIcon">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu, toggle classes based on menu state -->
  <div class="sm:hidden hidden" data-navbar-target="menu">
    <div class="px-2 pt-2 pb-3 space-y-1">
          <%= link_to "Services", services_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <%= link_to "Contact", contacts_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% if current_user && current_user.email == 'admin@gmail.com' %>
            <%= link_to 'Bookings', bookings_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% end %>
          <% if user_signed_in? %>
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: "text-indigo-800 hover:bg-gray-100  block px-3 py-2 rounded-md text-base poppins-regular" %>
          <% end %>
    </div>
  </div>
</nav>
